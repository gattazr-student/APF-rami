
let () =
	Printf.printf "Programme principal ici\n";;
	(* ALGO *)
	(* 	Il faut ouvrir les bonnes références vers les règles
		On fait une fonction qui demande à quel jeu veut jouer le joueur puis on instancie le module jeu et son foncteur avec ce choix
		on regarde aussi s'il y a un fichier en entrée pour charger le contenue d'une partie sauvegardée
		on appelle donc en fonction initialiser ou chargement
		puis joue
	Il faut faire quelque chose aussi pour sauvergarder la partie en fopnction de ce que peut nous renvoyer joué*)
	Printf.printf "Dans ce fichier, on appel le module Jeu pour jouer :-)\n";;
